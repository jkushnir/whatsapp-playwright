{
  "name": "whatsapp-playwright",
  "version": "1.0.0",
  "description": "Read and send WhatsApp messages from the command line via WhatsApp Web",
  "type": "module",
  "scripts": {
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
